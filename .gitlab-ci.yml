stages:
  - deploy

deploy_production:
  stage: deploy
  only:
    - main
  script:
    - echo "Deploying to production..."
    - docker compose down || true
    - docker compose up -d --build
    - docker compose ps