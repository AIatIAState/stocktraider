stages:
  - deploy

deploy_production:
  stage: deploy
  only:
    - main
  script:
    - cd /home/runner/app
    - git pull
    - docker compose down
    - docker compose up -d --build
  tags:
    - deployment